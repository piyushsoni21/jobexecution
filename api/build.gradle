dependencies {
    implementation (
            project(':service'),
            project(':persistence'),
            project(':security'),
            project(':common')
    )
    implementation 'org.springframework.boot:spring-boot-starter-web'
}