plugins {
    id "org.springframework.boot" version "$springBootVersion"
    id "io.spring.dependency-management"
    id 'java'
}
dependencies {
        implementation (
                project(':api'),
                project(':security'),
                project(':service')
        )
}
